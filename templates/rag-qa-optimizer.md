# 知识库问答 / RAG 优化模板

## 推荐架构

```text
输入理解 → 规则短路 → 混合检索 → 重排序 → 上下文组装 → 有依据回答 → badcase 反馈闭环
```

## 重点动作

1. 识别业务实体、指标、日期、系统名、文档名。
2. 高置信 FAQ / 规则问题优先确定性回答。
3. 结合关键词检索和向量检索。
4. 按文档权威性、时效性、匹配度重排序。
5. 回答必须基于证据，不得无依据扩展。
6. 记录 badcase，形成评测集和优化闭环。
